#cargo-features = ["edition2024"]
[package]
name = "enumy"
version = "0.1.69"
edition = "2024"

#[lib]
#crate-type = ["cdylib"] # cdylib oznacza C-Dynamic Library (.dll / .so)

[dependencies]
iced = { workspace = true, features = ["advanced", "svg"] }
strum = { workspace = true, features = ["derive"] }
image = { workspace = true }
libheif-rs = { workspace = true }
dirs = { workspace = true }
chrono = { workspace = true }
#futures = "0.3.32"

#libheif-rs = { workspace = true }